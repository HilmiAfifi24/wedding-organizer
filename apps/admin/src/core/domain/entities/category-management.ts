export const CATEGORY_MANAGEMENT_MENU_CODE = "CATEGORY_MANAGEMENT";

export interface CategoryPermissionFlags {
  canView: boolean;
  canInsert: boolean;
  canUpdate: boolean;
  canDelete: boolean;
}
